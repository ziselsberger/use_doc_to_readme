name: Update README.md

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo Hello, world!

  update-docu:
    uses: ziselsberger/doc_to_readme/.github/workflows/use_doc_to_readme.yml@main
